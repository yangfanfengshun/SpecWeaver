# SpecWeaver v0.5.1

## 本次更新

- 修复通过 `~/.local/bin/specweaver` 执行命令时无法定位公共 runtime 的问题。
- 安装后的 `specweaver update`、`specweaver status` 和认证配置命令现在会从真实脚本目录启动。

## 验证结果

- 自动化测试覆盖从符号链接启动终端命令的真实执行路径。
- Codex、Claude Code 和 Cursor 的插件清单与发布材料一致性检查通过。
