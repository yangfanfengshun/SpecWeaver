{
  "mcpServers": {
    "specweaver-tower": {
      "command": "/bin/bash",
      "args": ["${CURSOR_PLUGIN_ROOT}/scripts/run-mcp.sh", "tower"],
      "cwd": "${CURSOR_PLUGIN_ROOT}"
    },
    "specweaver-eolink": {
      "command": "/bin/bash",
      "args": ["${CURSOR_PLUGIN_ROOT}/scripts/run-mcp.sh", "eolink"],
      "cwd": "${CURSOR_PLUGIN_ROOT}"
    },
    "specweaver-lanhu": {
      "command": "/bin/bash",
      "args": ["${CURSOR_PLUGIN_ROOT}/scripts/run-mcp.sh", "lanhu"],
      "cwd": "${CURSOR_PLUGIN_ROOT}"
    }
  }
}
